# Klyrow IP Pools

Logical pools are `SHARED`, `DEDICATED`, and `CUSTOM`. Current delivery may map all logical pools to the existing Postal shared infrastructure. The value is still persisted and validated so future dedicated routing does not change tenant contracts.

Pool selection never bypasses sender, domain, quota, warm-up, suppression, reputation, or emergency-stop policy.
